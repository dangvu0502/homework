# Backend Configuration Example
# Copy this file to .env and update with your settings

# Redis Configuration
# Connection URL for Redis (used by Celery for task queue and results)
REDIS_URL=redis://localhost:6379/0

# AWS S3 Configuration
# AWS Access credentials
S3_ACCESS_KEY=your-aws-access-key-id
S3_SECRET_KEY=your-aws-secret-access-key
# S3 bucket name for storing uploaded images
S3_BUCKET_NAME=your-bucket-name
# AWS Region (e.g., us-east-1, us-west-2)
S3_REGION=your-region

# Database Configuration
# PostgreSQL connection URL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ui_annotations

# API Server Configuration
# Host to bind the API server
API_HOST=0.0.0.0
# Port for the API server
API_PORT=8000

# Worker Configuration
# Number of concurrent Celery workers
WORKER_CONCURRENCY=4
# Queue size threshold for triggering auto-scaling
QUEUE_THRESHOLD_FOR_SCALING=100

# Model Configuration
# Path to the model configuration YAML file
MODEL_CONFIG_PATH=model_config.yaml

# LLM API Keys (add these based on your model_config.yaml)
# OPENAI_API_KEY=your-openai-api-key
# ANTHROPIC_API_KEY=your-anthropic-api-key
# GEMINI_API_KEY=your-gemini-api-key